# /docs

Design and operational documentation for Citadel.

## Planned files

- `architecture.md` — component diagram, data flow, event schema, why eBPF
- `threat-model.md` — adversary capabilities, what Citadel catches vs. what it doesn't, blast-radius assumptions
- `DEMO.md` — beat-by-beat live demo script (3 attacks in ~5 minutes), reset procedure, fallback strategies
- `local-demo.md` — running Citadel end-to-end with locally built images (no GHCR dependency)

The 24-hour build plan that generated this repo lives at the project root as `citadel-build-plan.md`.
